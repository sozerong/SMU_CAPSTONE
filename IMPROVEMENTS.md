# 개선 작업 기록 — VCC (SMU_CAPSTONE), 2026년 8월

측정 조건과 원본 수치는 `BENCHMARK.md` / `spark/bench_results/*.json` 참조.

---

## 조사 결과 — 포트폴리오 기술 내용이 코드에 없었다

작업 전, 포트폴리오에 적힌 항목이 저장소에 실제로 있는지부터 확인했다.

| 포트폴리오 기술 | 코드에 존재하는가 |
|---|---|
| 유튜브 키워드 ↔ 서울시 카페 메뉴 **조인** | **없음** — 카페 메뉴 데이터 자체가 저장소에 없다 |
| 메뉴명 정규화 (사이즈/온도/옵션 제거) | **없음** |
| 조인 후 키워드 단위 groupBy 집계 | **없음** |
| salting / broadcast join 힌트 | **없음** |

Spark 코드는 [`spark/spark_keyword.py`](spark/spark_keyword.py) 하나뿐이고,
Kafka에서 유튜브 제목·설명을 읽어 TF-IDF로 키워드를 뽑는 게 전부다.
`.join(` 도 `groupBy` 도 `broadcast` 도 `salt` 도 한 번도 나오지 않는다.

Okt 명사 추출은 [`agent/food_fillter.py`](agent/food_fillter.py) 에 있지만
**유튜브 키워드**를 대상으로 하는 것이고, 메뉴명 정규화(사이즈/온도/옵션 표기 제거)와는 다르다.

**→ 아래 작업은 기존 프로젝트의 성과가 아니라, 이번에 새로 만든 것이다.**
포트폴리오에 기존 성과로 쓰면 안 된다.

---

## 작업 — 조인 파이프라인 신규 구현 + 스큐 대응

① **문제**: 조인 파이프라인이 없다. "유튜브에서 뜨는 키워드를 서울 카페들이 이미
얼마나 팔고 있나"를 답하려면 키워드와 메뉴를 붙여야 하는데, 붙이는 코드가 없었다.

붙이려고 보니 두 가지가 걸린다.
- 메뉴명이 `(ICE) 바닐라 라떼 L 샷추가` 같은 형태다. 이대로면 같은 메뉴가
  수십 가지 문자열로 흩어져 조인이 걸리지 않는다.
- 메뉴 분포가 롱테일이다. 아메리카노·라떼는 거의 모든 카페에 있고 나머지는 희소하다.
  → 조인/집계 키가 쏠린다.

② **확인**: 데이터가 없어서 만들었다 (아래 ③ 참조). 만든 데이터에서 실제로 잰 쏠림:

| 데이터 | 파티션 최대 | 중앙값 | max/median |
|---|---|---|---|
| 216,441행 | 18,842 | 3,934 | **4.79** |
| 1,296,229행 | 113,414 | 23,808 | **4.76** |

`mapPartitionsWithIndex` 로 파티션별 레코드 수를 직접 세서 얻은 값이다
(`partition_sizes()` in [`spark/menu_join.py`](spark/menu_join.py)).

③ **접근**:

*데이터* — 서울시 공공데이터에 **메뉴명 단위 데이터셋이 없다.**
일반음식점 인허가 정보는 상호·주소까지고 메뉴는 없다. 그래서
[`spark/make_cafe_menu.py`](spark/make_cafe_menu.py) 로 **합성 데이터**를 만들었다.
기본 메뉴명은 저장소의 실제 키워드에서 가져와 조인이 실제로 걸리게 했고,
사이즈/온도/옵션 표기와 롱테일 분포만 현실을 흉내냈다. **합성이라는 사실을
데이터 생성기와 BENCHMARK.md 양쪽에 명시했다.**

*정규화* — Okt(konlpy)를 쓰려 했으나 JPype 의존성 때문에 이 환경에 설치되지 않았다.
Okt를 우선 쓰고 **없으면 정규식 폴백**으로 떨어지는 구조로 만들고,
어느 쪽을 썼는지 출력에 찍게 했다. 이번 측정은 폴백 기준이다.

*조인* — 키워드 테이블이 6,494행뿐이라 Spark가 자동으로 broadcast 해버린다.
힌트의 효과를 보려면 `autoBroadcastJoinThreshold=-1` 로 자동 broadcast를 꺼야
비교가 성립한다. AQE도 껐다 — 켜두면 스큐를 자동 완화해서 salting 효과가 측정되지 않는다.

④ **해결**: 새 파일 3개.
- [`spark/menu_normalize.py`](spark/menu_normalize.py) — 사이즈/온도/옵션/용량 표기 제거 +
  명사 추출. 사이즈 토큰은 단독일 때만 지운다 (`카페 라떼 R` 의 R은 지우되
  단어 안의 글자는 건드리지 않게). `demo()` 자체 검증 포함 — 표기가 달라도
  같은 조인 키가 나오는지 assert 한다.
- [`spark/make_cafe_menu.py`](spark/make_cafe_menu.py) — 합성 메뉴 데이터 생성.
- [`spark/menu_join.py`](spark/menu_join.py) — 파이프라인 본체.
  `--mode plain|broadcast|salt`, `mapPartitionsWithIndex` 파티션 편차 측정,
  `explain()` 결과에서 실제 조인 방식 추출, salt는 **빈도 상위 N개 키에만** 부착
  (전체에 붙이면 재집계 비용만 늘어난다).

⑤ **결과** (각 3회 p50, 상세는 `BENCHMARK.md`)

**1,296,229행 기준**

| 모드 | 조인 방식 | 총 소요 p50 | max/median |
|---|---|---|---|
| plain | SortMergeJoin | **44.44s** | 4.76 |
| broadcast | BroadcastHashJoin | **47.96s** | 4.76 |
| salt (상위 5키 × 16버킷) | BroadcastHashJoin | **62.03s** | **2.39** |

- **broadcast 힌트는 효과가 없었다.** `explain()` 상 조인 방식은 SortMergeJoin →
  BroadcastHashJoin 으로 확실히 바뀌었는데도 8% 느려졌다. 216k에서는 1% 빨라졌는데
  회차 편차 안이다. 키워드 쪽이 작아서 어느 쪽이든 셔플이 지배적이지 않다.
- **salting은 파티션 편차를 실제로 절반으로 줄였다** (4.76 → 2.39).
  **그런데 총 소요는 40% 늘었다.** 2단계 집계의 `collect_set` → `flatten` →
  `array_distinct` 비용이 줄인 편차로 얻는 것보다 컸다.
- 즉 **이 규모에서 스큐는 병목이 아니다.** 최대 파티션이 11만 행이어도
  16스레드가 그걸 처리하는 시간이 전체를 지배하지 않는다.

**남은 한계**
- 단일 노드 `local[*]` 기준이다. 실제 클러스터에서는 네트워크 셔플 비용이 커서
  broadcast의 이득이 더 클 수 있다. 이 측정으로 "broadcast는 쓸모없다"고
  일반화하면 틀린다.
- 카페 메뉴가 합성 데이터라 쏠림 4.8배가 실제 분포와 같다는 보장이 없다.
  salting이 이기는 지점을 찾으려면 편차가 훨씬 큰 실제 데이터가 필요하다.
- 정규화가 regex 폴백이라 Okt 기준과 매칭 결과가 다를 수 있다
  (매칭 키워드 68종은 폴백 기준 수치다).
- `spark_keyword.py` (기존 TF-IDF 파이프라인)는 손대지 않았다. 이번 작업은
  그 산출물(`keywords.jsonl`)을 입력으로 받는 별도 단계다.
